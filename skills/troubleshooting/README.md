# Troubleshooting skill candidates

This directory is for reviewed, generic troubleshooting runbooks promoted from repeated harness error signatures.

Do not store project-specific assumptions here; keep those in `profiles/<project>.local.ps1` or a project runbook.
